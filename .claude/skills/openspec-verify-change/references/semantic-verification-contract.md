# Independent semantic verification

Read actual current source, ACCEPT/TEST and retained product evidence after the
actual dispatched writer set has joined. The verifier identity and context must
be independent from implementation. Required mechanical checks, semantic direction,
input/code/config/obligation freshness and complete evidence are all necessary.
A later valid FAIL defeats an old PASS. Missing Run for CLI, empty observations,
blocking reasons or stale evidence cannot be silently treated as success.

Return receipt_ref plus action-specific result; the controller derives binding IDs.
Task semantic results use verdict, reasons, evidence_refs and duration_seconds.
Do not edit source, tests, manifest, notebook, product or ledger to make a check pass.
DEVIATED results identify expected versus observed direction and evidence.

Stage closure checks its mandatory subtree and explicit predecessors. Root complete
requires current structural/design/final and applicable production evidence,
planned_mandatory_remainder=[] and unplanned_root_obligations=[], no unresolved
repair/planning or active/unconsumed claims. Production not_applicable needs the
accepted contract reason. Finalizer verification precedes parent closure.

Translation is one derived translation.zh-CN.md companion of proposal/design/tasks,
with complete source_anchor and byte SHA256 mapping. Refresh after final source
promotion as non-Apply work, without mutating source and creating another cycle.
Real spec/goal/apply and native overlap/independent closure cannot be proved by
source tests, fake events or an installed label. No mandatory ablation matrix.
