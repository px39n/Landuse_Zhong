# Current source interview packet

Record the named problem, expected/observed outcome and source anchors, confirmed
business decisions, remaining material unknowns, scope and forbidden writes,
retention/paths, measurable acceptance and proposed dependency/Goal relations.
Reuse existing answers and the current budget_lineage_id. Do not turn an
explicitly authorized implementation correction into another approval interview.

The compact Loop routes `next_action=interview` here for source authoring;
do not invent a planning receipt, semantic_delta or another submission command.
Validate the source/index and return the changed contract anchors and unresolved
decisions to the supervisor. Coordinate affected active claims through the
controller. Source authoring does not establish installed/runtime PASS.
No packet is a new grant or a budget reset. Business choices beyond delegated
authority require the user; mechanical derived criteria keep their source proof.
