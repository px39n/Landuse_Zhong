# Artifact placement

Reuse an explicit Artifact Retention Decision. If absent, ask once and wait before
proposal/design/specs/tasks authoring. It covers none|thin|full, retained evidence
root, disposable cache/pytest/scratch, product/runtime outputs and GUI/Colab paths.
No conventional root is selected by silence. Authoring creates no cache.

Keep one current contract and structure.md index; handoff.json is optional advisory
metadata, never a second ledger. Generated evidence belongs only in approved roots.
translation.zh-CN.md is the single derived proposal/design/tasks companion with
source_anchor and source byte SHA256; preserve protected tokens and quantities.
Retain raw history without executable compatibility or extra documentation variants.
Platform metadata and nonselected skills MUST NOT be deleted during synchronization.
